# CLI Bookmark Manager

A simple command-line application written in Go that allows users to manage bookmarks as key-value pairs.

## Features
- View all bookmarks
- Add a new bookmark (key and value)
- Delete a bookmark by key
- Simple menu-driven interface

## How to Run
`go run main.go`