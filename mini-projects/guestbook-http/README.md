# Guestbook HTTP

A simple web application written in Go that allows users to sign a guestbook.  
Signatures are stored in a text file `data/signatures.txt`.

## Features
- View list of signatures
- Add a new signature via form
- Simple file-based storage

## How to Run
`go run main.go`
Open in browser: http://localhost:8080/guestbook 